import { createAsyncThunk, createSlice } from "@reduxjs/toolkit";

export const asyncCounterSlice = createSlice({
    name: 'asyncCounter',
    initialState: {
        value: 0
    },
    reducers: {
        
    }
})